; Sample Analog_Bridge configuration (placeholders)

[GENERAL]
logLevel=1

[USRP]
bindPort=32001
remotePort=32002
remoteAddress=analog_reflector

[AMBE]
; VOCODER_MODE: SOFT (no external codec) or HARD (requires hardware/codec libs)
vocoder=SOFT
audioSampleRate=8000

